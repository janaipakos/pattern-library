{
  "name": "pattern-library",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf docs/",
    "build": "npm run clean && kss --config kss-config.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "static-server -p 8000"
  },
  "keywords": [],
  "author": "James Anaipakos",
  "license": "ISC",
  "devDependencies": {
    "kss": "^3.0.0-beta.18"
  },
  "dependencies": {
    "static-server": "^2.0.4"
  }
}
